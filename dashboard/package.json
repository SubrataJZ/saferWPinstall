{
  "name": "saferwp-dashboard",
  "version": "1.0.0",
  "description": "Central dashboard for SaferWP Install: tracks managed WordPress sites, scans installed plugins/themes against the WPScan vulnerability database, and recommends safe versions.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "node-fetch": "^2.7.0"
  }
}
